# Python runtime requirements for C³ / Cactus PoC (pip format)
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
matplotlib>=3.3
scapy>=2.4.5
# Optional: Python bindings for BPF. On Debian/Ubuntu install system package:
# sudo apt install python3-bcc
# If you attempt via pip, the package name is 'bcc' but system libs are usually required.
bcc>=0.19.0

