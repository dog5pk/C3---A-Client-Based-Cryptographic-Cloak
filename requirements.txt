# requirements.txt -- Python runtime requirements for C³ / Cactus PoC
numpy>=1.21
pandas>=1.3
scikit-learn>=1.0
matplotlib>=3.3
scapy>=2.4.5

# NOTE: 'bcc' (Python bindings for eBPF) is intentionally NOT listed here because
# it commonly requires kernel headers and system libs.  To use BPF features:
#  - install system package on a real Linux VM: sudo apt install python3-bcc
#  - or run experiments on a machine that has the matching linux-headers installed
